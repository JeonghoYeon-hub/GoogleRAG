# Web server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.20
pydantic>=2.10.0

# Environment variables
python-dotenv>=1.0.0

# Google Gemini File Search
google-genai>=1.30.0

# Google Drive OAuth + API
google-auth>=2.40.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.150.0

# Notion
notion-client>=2.2.1

# SVG to PDF (requires reportlab, tinycss2, cssselect2)
svglib>=1.5.1
reportlab>=4.0.0

# Excalidraw rendering via Playwright + Chromium
# After pip install, download Chromium separately:
#   python -m playwright install chromium
# setup.bat handles this automatically
playwright>=1.50.0

# Excalidraw compressed-json decode
lzstring>=1.0.4
